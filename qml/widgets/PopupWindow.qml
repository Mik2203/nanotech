// import QtQuick 1.0 // to target S60 5th Edition or Maemo 5
import QtQuick 1.1
import Window 0.1

Window {
    id: window
//    x: mainWindow.x + container.mapToItem(mainWindow, 0, container.height).x
//    y: /*container.y + container.height + */mainWindow.y + container.mapToItem(mainWindow, 0, container.height).y
//            anchors.top: container.bottom
//            anchors.left: container.left
    height: 200
    width: 200
    //windowFlags: Qt.Popup
    //deleteOnClose: false
    //visible: expanded


}
